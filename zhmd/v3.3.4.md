<table align="center">
  <tr>
    <td align="center" bgcolor="#e5e7eb" width="88" height="36"><a href="https://github.com/ussoewwin/ComfyUI-HSWQ-Loader-and-Tools/releases/tag/v3.3.4"><font color="#4b5563"><b>EN</b></font></a></td>
    <td align="center" bgcolor="#3478ca" width="88" height="36"><font color="#ffffff"><b>中文</b></font></td>
  </tr>
</table>

## ① 何が問題か

Z Image / ZIT **ConvRot NVFP4**（および同一 Comfy parity オンライン act rotate 経路上の INT8 protect Linear）は、各モジュールに Hadamard 行列 `module._hswq_nvfp4_parity_H` を保持し、毎 forward で再利用する。

**Distorch** VRAM purge のあと、Method 3 は tensor storage を空／毒化する（`t.data = empty` 系）一方で、Python 側の属性参照は残ることがある。`nodes/nvfp4/nvfp4_hadamard.py` の**グローバル** Hadamard cache は、再利用前に `_tensor_storage_ok` で毒化テンソルを既に拒否していた。一方 `_make_convrot_parity_forward` の**モジュールローカル**経路はそれを使っていなかった。見ていたのは:

- `h.device` / `h.dtype` と `input` の一致
- `h.numel() == 0`
- CUDA / 非 CUDA の不一致
- 後付けで `storage.nbytes() == 0`

そのため device/dtype が揃って見える殻（さらに `_tensor_storage_ok` なら落とすが旧判定では通る殻）を再利用し得る。オンライン act rotate が死んだ／ゴミの `H` で回り、1 回目は正常に見え、purge 後の**2 回目以降**で劣化する（ノイズ／画質低下）。clear 経路のコメントにある 2nd→3rd→4th の段階的悪化と同じ構図。

**v3.3.4 以前の壊れた再利用判定**（`forward_parity` 内）:

```python
            h = getattr(self, "_hswq_nvfp4_parity_H", None)
            need_rebuild = True
            if h is not None:
                try:
                    need_rebuild = (
                        h.device != input.device
                        or h.dtype != input.dtype
                        or int(h.numel()) == 0
                        or (bool(input.is_cuda) and not bool(h.is_cuda))
                    )
                    if not need_rebuild:
                        # DistOrch Method 3 may empty storage while device/dtype
                        # still match — rebuild or 2nd gen becomes noise.
                        st = (
                            h.untyped_storage()
                            if hasattr(h, "untyped_storage")
                            else h.storage()
                        )
                        need_rebuild = int(st.nbytes()) == 0
                except Exception:
                    need_rebuild = True
```

---

## ② 追加・修正したファイル名

| パス | 変更内容 |
|------|----------|
| `nodes/zimage_nvfp4/nvfp4_comfy_parity.py` | モジュールローカル `H` 再利用で `_tensor_storage_ok` を呼ぶ。関連コメントの製品表記を **Distorch** に修正 |
| `nodes/nvfp4/nvfp4_hadamard.py` | docstring の `DistOrch` → **Distorch**（関数本体は従来どおり。グローバル cache が既に使用） |
| `pyproject.toml` | 版 `3.3.3` → `3.3.4` |
| `__init__.py` | `__version__` `3.3.3` → `3.3.4` |
| `changelog.md` | Version **3.3.4** 追記 |
| `zhmd/CHANGELOG.md` | Version **3.3.4** 追記 |
| `zhmd/v3.3.4.md` | 中文リリースノート（本ファイル） |
| `release/_release_v3.3.4_body_en.md` | 英文 GitHub Release 本文 |

修正用の新規 `.py` は追加していない。挙動修正は `nvfp4_comfy_parity.py`（＋版／ドキュメント面）のみ。

---

## ③ 追加・修正したコード全文

### 3.1 `nodes/nvfp4/nvfp4_hadamard.py` — `_tensor_storage_ok`（parity が共有するゲート）

```python
def _tensor_storage_ok(t) -> bool:
    """False after Distorch nuclear kill / empty-storage reuse (UAF risk)."""
    if t is None:
        return False
    try:
        if int(getattr(t, "numel", lambda: 0)()) <= 0:
            return False
        st = t.untyped_storage() if hasattr(t, "untyped_storage") else t.storage()
        if int(st.nbytes()) <= 0:
            return False
        # Shape must match a square Hadamard (or 4x4 h4); reject emptied shells.
        if getattr(t, "ndim", 0) == 2:
            if int(t.shape[0]) != int(t.shape[1]) or int(t.shape[0]) < 4:
                return False
        return True
    except Exception:
        return False
```

### 3.2 `nodes/nvfp4/nvfp4_hadamard.py` — `clear_hadamard_global_caches` docstring（表記のみ）

```python
def clear_hadamard_global_caches() -> int:
    """Drop module-level Hadamard caches (Distorch Method 2c / parity clear).

    Method 3 may ``t.data = empty`` on tensors still referenced by these dicts.
    Returning them on the next gen rotates with dead/garbage ``H`` and quality
    decays as CUDA reallocates the freed region (2nd→3rd→4th gen worse).
    """
    n = len(_HADAMARD_CACHE) + len(_H4_CACHE)
    _HADAMARD_CACHE.clear()
    _H4_CACHE.clear()
    return n
```

### 3.3 `nodes/zimage_nvfp4/nvfp4_comfy_parity.py` — 修正後の `_make_convrot_parity_forward` 全文

```python
def _make_convrot_parity_forward(stock_forward):
    """Stock MixedPrecision forward + online act rotate for ConvRot Linears.

    NVFP4: ``_hswq_nvfp4_convrot`` (Params.convrot cleared at load).
    INT8 protect: ``_hswq_int8_convrot`` (Params.convrot cleared at load —
    same as Conv2d). Kitchen must **not** see Params.convrot=True or
    int8_linear double-rotates with this path.
    """
    from ..nvfp4.nvfp4_hadamard import build_hadamard, rotate_last_dim

    def forward_parity(self, input, *args, **kwargs):
        nv = bool(getattr(self, "_hswq_nvfp4_convrot", False))
        i8 = bool(getattr(self, "_hswq_int8_convrot", False))
        if nv or i8:
            if nv:
                gs = int(getattr(self, "_hswq_nvfp4_convrot_groupsize", 256) or 256)
            else:
                gs = int(getattr(self, "_hswq_int8_convrot_groupsize", 256) or 256)
            h = getattr(self, "_hswq_nvfp4_parity_H", None)
            need_rebuild = True
            if h is not None:
                try:
                    from ..nvfp4.nvfp4_hadamard import _tensor_storage_ok

                    # Global cache already rejects poisoned H via
                    # _tensor_storage_ok; module-local H must use the same
                    # check. nbytes==0 alone misses emptied shells that still
                    # report device/dtype (Distorch Method 3), so 2nd+ gen
                    # rotates with garbage and quality decays.
                    need_rebuild = (
                        h.device != input.device
                        or h.dtype != input.dtype
                        or (bool(input.is_cuda) and not bool(h.is_cuda))
                        or not _tensor_storage_ok(h)
                    )
                except Exception:
                    need_rebuild = True
            if need_rebuild:
                h = build_hadamard(gs, device=input.device, dtype=input.dtype)
                self._hswq_nvfp4_parity_H = h
            input = rotate_last_dim(input, h, gs)
        return stock_forward(self, input, *args, **kwargs)

    forward_parity._hswq_nvfp4_convrot_parity = True  # type: ignore[attr-defined]
    return forward_parity
```

### 3.4 同一 parity ファイル内の関連 docstring／コメント表記（挙動変更なし）

`clear_nvfp4_parity_hadamard_caches` の docstring・コメント、および `_unwrap_stock_forward` の docstringを **Distorch** 表記に修正（本体ロジックは不変）。

---

## ④ その意味

| 要素 | 意味 |
|------|------|
| `_tensor_storage_ok(t)` | `t` が欠落、空（`numel` / `nbytes`）、正方 Hadamard として不正な shape、または検査中に例外なら False。Distorch Method 3 が storage を空／壊したあとも Python 参照が残るケースを落とす。 |
| グローバル `build_hadamard`（既存） | cache が毒なら返さず、`_tensor_storage_ok` 失敗時に作り直す。 |
| 旧モジュール判定 | device/dtype が揃い `nbytes != 0` なら `_hswq_nvfp4_parity_H` を残し得た。`_tensor_storage_ok` なら落とす殻でも再利用 → purge 後 **2 回目以降**でゴミ `H` による `rotate_last_dim`。 |
| 新 `need_rebuild = … or not _tensor_storage_ok(h)` | モジュールローカル再利用を**グローバルと同じ生存判定**に揃える。毒なら `build_hadamard` で作り直し `_hswq_nvfp4_parity_H` を書き直す。 |
| 単独の `numel==0`／後段 `nbytes==0` 分岐の削除 | `_tensor_storage_ok` 内の `numel`／`nbytes`／shape で足りる。グローバルと薄い別ポリシーを二重に持たない。 |
| `rotate_last_dim(input, h, gs)` | 契約は不変（NVFP4／INT8 protect ConvRot のオンライン act rotate）。変わったのは **どの `h` を信用するか** だけ。 |

### 操作説明

1. タグ **v3.3.4** に更新する。
2. ComfyUI を完全再起動する。
3. HSWQ NVFP4 / INT8 使用時は末尾に **General Purge VRAM V2**（**`HSWQ`** オン）を維持する。
4. purge 後の 2 回目以降が、毒化したモジュールローカル Hadamard 再利用で劣化しないことを確認する。

### 互換性

| 項目 | 方針 |
|------|------|
| 範囲 | **HSWQ ConvRot INT8/ConvRot NVFP4 UNet Loader** 上の Z Image / ZIT **ConvRot NVFP4**／INT8 protect オンライン rotate |
| 量子化器 | **のみ** [Hybrid-Sensitivity-Weighted-Quantization](https://github.com/ussoewwin/Hybrid-Sensitivity-Weighted-Quantization) |
| SDXL ConvRot NVFP4 | 不変（Checkpoint Loader + `nodes/nvfp4` TC 製品経路） |
| ComfyUI-master | 本拡張は変更しない |
